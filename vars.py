mMin = 0.15 # Minimum amount of minutes a song must play before it counts as a song 

getDownloadedData = True #Will get downloaded data from Spotify Account Data folder

sortBy = 0 #If 0, then sort by tracks played, if 1, then sort by minutes played